# Settings optimized for herbarium specimens with degraded DNA
specimux-suite-version: "*"   # bundled: always the installed version
description: "Herbarium specimens with relaxed consensus thresholds"

suite:
  min-reads: 10
  reprocess-ratio: 0.3

speconsense:
  profile: herbarium
