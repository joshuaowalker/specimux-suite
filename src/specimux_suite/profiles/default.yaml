# Default suite profile — matches built-in defaults
specimux-suite-version: "*"   # bundled: always the installed version
description: "Default pipeline settings"

suite:
  min-reads: 10
  reprocess-ratio: 0.5
